version: '3.7'

services:
  web:
    build: ./
    command: poetry run uvicorn schoolsyst_api.main:api --reload --workers 1 --host 0.0.0.0 --port 8000
    volumes:
      - ./:/usr/src/app
    ports:
      - 8002:8000
    environment:
      - ENVIRONMENT=dev
      - TESTING=0
